name: talent_foot_connect
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  file_picker: ^11.0.3
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  image_picker: ^1.2.2
  supabase_flutter: ^2.17.2
  video_player: ^2.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/bg-stadium.jpg
